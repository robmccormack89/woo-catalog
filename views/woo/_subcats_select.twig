{% for term in subcat_sel_terms %}
  <label><input class="uk-checkbox uk-border-rounded" type="checkbox" name="product_cat" id="{{ term.term_id|e('esc_html') }}" value="{{ term.slug|e('esc_html') }}"> {{ term.name|e('esc_html') }}</label>
{% endfor %}